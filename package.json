{
  "name": "@buggyorg/muxcontinuations",
  "version": "0.1.6",
  "description": "Adds continuations to multiplexers.",
  "main": "./lib/api.js",
  "scripts": {
    "test": "standard src/**/*.js && mocha --compilers js:babel-register",
    "build": "babel src -d lib",
    "prepublish": "babel src -d lib"
  },
  "author": "Maximilian Klein",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "chai": "^3.5.0",
    "mocha": "^2.4.5",
    "standard": "^6.0.8"
  },
  "dependencies": {
    "@buggyorg/graphtools": "^0.3.0",
    "lodash": "^4.13.1"
  }
}
